#include "config.h"

Package {
	name		= "unix",
#ifdef INSTALLING
	import_dirs	= [ "$libdir/imports/unix" ],
#else
	import_dirs	= [ "$libdir/libraries/unix" ],
#endif
	source_dirs	= [],
#ifdef INSTALLING
	library_dirs	= [ "$libdir" ],
#else
	library_dirs	= [ "$libdir/libraries/unix" ],
#endif
	hs_libraries	= [ "HSunix" ],
	extra_libraries	= [],
        include_dirs	= [],
        c_includes	= [],
        package_deps	= [ "base" ],
        extra_ghc_opts	= [],
        extra_cc_opts	= [],
        extra_ld_opts	= []
}
